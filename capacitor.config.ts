import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-ui-components-showcase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
